[package]
name = "tracing-demo"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = [
    "fmt",
    "env-filter",
    "registry",
] }
tracing-error = { version = "0.2" }
anyhow = "1.0"
tokio = { version = "1", features = ["full"] }
